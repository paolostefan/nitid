## print(string s)  
  
Outputs a string (its argument).  
## println(string s)  
  
Outputs a string (its argument), followed by a newline. Does not support formatting.  
## printf(string fmt, any args[])  

Exactly the same as C std library's `printf()`.
