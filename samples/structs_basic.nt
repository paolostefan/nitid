struct Point {
  x: i32;
  y: i32;
};

impl Point {
  fn sum -> i32 {
    return self.x + self.y;
  }
};

fn main {
  p := Point{ x: 3, y: 4 };
  printf("x=%d y=%d\n", p.x, p.y);
  printf("sum=%d\n", p.sum());
}